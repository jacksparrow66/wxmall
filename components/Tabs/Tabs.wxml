<view class="tabs">
    <view class="goods_title">
        <view class="title {{tabs[index].status?'active':''}}" wx:for="{{tabs}}" wx:key="id" bindtap="Tab_click" data-index="{{index}}">
            {{item.title}}
        </view>
    </view>
    <view class="goods_conents">
        <slot></slot>
    </view>
</view>